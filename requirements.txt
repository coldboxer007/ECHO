# ECHO Robot - Requirements
# Install with: pip install -r requirements.txt

# ── Gemini AI ──
google-genai>=1.0.0

# ── Speech-to-Text (Faster Whisper - optimized for RPi) ──
faster-whisper>=1.0.0

# ── Audio ──
pyaudio>=0.2.14
numpy>=1.24.0

# ── Camera & Vision ──
opencv-python-headless>=4.8.0

# ── TFLite Runtime (for Raspberry Pi) ──
# NOTE: Official PyPI wheels may not cover all RPi / Python combos.
# If `pip install tflite-runtime` fails, try the Coral repo:
#   pip install --index-url https://google-coral.github.io/py-repo/ tflite_runtime
tflite-runtime>=2.14.0

# ── Face Display ──
pygame>=2.5.0

# ── GPIO (Raspberry Pi) ──
# RPi.GPIO is pre-installed on Raspberry Pi OS
# Uncomment below if needed:
# RPi.GPIO>=0.7.1

# ── Environment variables ──
python-dotenv>=1.0.0
